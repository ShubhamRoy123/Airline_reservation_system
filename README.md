# Airline Reservation System

Simple C++ console program for airline reservations.

Build:
- Use the provided VS Code task (C/C++: cl.exe build active file) or run `cl.exe` manually.

Notes:
- This repo is intended to be pushed to `https://github.com/ShubhamRoy123/Airline_reservation_system.git`.
